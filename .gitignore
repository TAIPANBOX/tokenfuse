# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Node / dashboard
node_modules/
.next/
dist/
build/
*.tsbuildinfo

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Local data / models
/data
*.parquet
*.onnx
*.duckdb

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/

# radar (Linux eBPF) build artifacts
crates/radar/target/
crates/radar/**/target/

# cluster (excluded crate, own target dir)
crates/cluster/target/

# Workflow worktrees are transient checkouts of this repo, created by the
# orchestrator. They are embedded git repositories and must never be committed:
# a `git add -A` during a running workflow will otherwise sweep them in.
.claude/worktrees/
