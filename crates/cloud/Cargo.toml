[package]
name = "tokenfuse-cloud"
version = "0.0.1"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "TokenFuse Cloud control plane: aggregates gateway telemetry into a per-org fleet view."

[[bin]]
name = "tokenfuse-cloud"
path = "src/main.rs"

[dependencies]
tokenfuse-core.workspace = true
# RFC 9449 proofs, RFC 7638 thumbprints and the one algorithm allowlist,
# shared with the gateway rather than copied into it (invariant 29).
tokenfuse-dpop.workspace = true
tokenfuse-delegation.workspace = true
tokio = { workspace = true, features = ["sync", "time"] }
tokio-stream = { version = "0.1", features = ["sync"] }
axum.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
utoipa = { version = "5", features = ["axum_extras"] }
# Device pairing + hardware-backed signed mutations, and the signed audit
# manifest (ES256 = ECDSA P-256/SHA-256). `pkcs8`/`pem` let the audit signing
# key load from a PKCS#8/SEC1 PEM or DER (see `audit_sign.rs`).
p256 = { version = "0.13", features = ["ecdsa", "pkcs8", "pem"] }
sha2.workspace = true
base64.workspace = true
getrandom = "0.2"
# Offline OIDC / JWT bearer auth (WS4): RS256/ES256 signature + claim validation
# against a static JWKS. No network — the JWKS is supplied from env. Cloud-only;
# never pulled into tokenfuse-core.
jsonwebtoken.workspace = true
# APNs push (real sender) — only pulled in with `--features apns`.
reqwest = { version = "0.12", optional = true, default-features = false, features = [
    "json",
    "rustls-tls",
    "http2",
] }

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
http-body-util = "0.1"
# The signed-audit-manifest tests re-derive the canonical bytes (core) and
# verify the ES256 signature independently (p256), exactly as an auditor would.
tokenfuse-core.workspace = true
p256 = { version = "0.13", features = ["ecdsa"] }
base64 = "0.22"

[features]
# Build the real APNs client; needs `p256` PKCS#8/PEM for the provider JWT key
# (already enabled unconditionally above for the audit-manifest signing key).
apns = ["dep:reqwest"]
